# HABITUS QGIS Plugin - Python Dependencies
# Install via OSGeo4W Shell (Windows) or terminal (macOS/Linux):
#   pip install -r requirements.txt

# Core requirements (mandatory)
scikit-learn>=1.0
rasterio>=1.3
numpy>=1.21
pandas>=1.3
scipy>=1.7

# Visualization (strongly recommended)
matplotlib>=3.5

# Optional algorithm libraries
xgboost>=1.6          # XGBoost algorithm
lightgbm>=3.3         # LightGBM algorithm
catboost>=1.0         # CatBoost algorithm
pygam>=0.8            # Generalised Additive Model
elapid>=0.6           # MaxEnt (presence-only modelling)
